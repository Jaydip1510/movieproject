<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../css/index.css">
    <title>movie</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-4 p-5 bg-secondary">
                <form action="/savedata" method="post" name="frm" enctype="multipart/form-data">
                    <input type="hidden" name="hid" value="<%= (user.id!='')?user.id:''%>">
                    <label for=""><b>Movie Name:-</b></label>
                    <input type="text" name="name"><br><br>

                    <label for=""><b>Release Date:-</b></label>
                    <input type="date" name="date" value="<%= (user.id!='')?user.id:''%>?user.date:'' %>"><br><br>

                    <label for=""><b>Chractors:-</b></label>
                    <input type="number" name="charactor"
                        value="<%= (user.id!='')?user.id:''%>?user.charactor:'' %>"><br><br>

                    <label for=""><b>Poster Image:-</b></label>
                    <input type="file" name="image"><br><br>

                    <input type="submit" class="btn btn-success" value="save">

                </form>
            </div>
            <div class="col-8 p-5 bg-warning  d-flex justify-content-center align-items-center">

                <br><br>

                <table border="2" class="table table-striped">
                    <tr align="center">

                        <th>id</th>
                        <th>Name</th>
                        <th>Release Date</th>
                        <th>Charactor</th>
                        <th>Image</th>
                        <th>Action</th>

                    </tr >
                    <% for(let i in data){ %>
                        <tr align="center">
                            <td>
                                <%= data[i]._id %>
                            </td>
                            <td>
                                <%= data[i].moviename %>
                            </td>
                            <td>
                                <%= data[i].date %>
                            </td>
                            <td>
                                <%= data[i].charactor %>
                            </td>

                            <td><img src="../<%= data[i].pimage %>" height="100px" width="100px"></td>
                            <td><a href="/edit/<%= data[i]._id%>" class="btn btn-primary">Edit</a> <a href="/del/<%= data[i]._id%>" class="btn btn-danger">Delete</a>
                            </td>



                        </tr>
                        <%} %>
                </table>
            </div>
        </div>
    </div>
</body>

</html>